# 文件名: .github/workflows/sync-meta.yml
# 日常使用的同步工作流 - 与源仓库完全一致

name: Sync Meta Folders Only

on:
  schedule:
    - cron: '30 18 * * *'  # UTC 18:30 = 北京时间第二天 2:30
    - cron: '30 6 * * *'   # UTC 6:30 = 北京时间 14:30
  workflow_dispatch:     # 无强制选项，每次都完整镜像同步

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 1

      - name: Setup Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Delete old meta folder (with tolerance)
        run: |
          echo "🗑️ 开始删除旧的 meta 文件夹（容错处理）..."
          
          if [ -d "meta" ]; then
            echo "找到 meta 目录，正在强制删除..."
            rm -rf meta
            echo "✅ meta 目录已成功删除"
          else
            echo "ℹ️ meta 目录不存在，无需删除"
          fi
          
          echo "删除后工作区确认:"
          ls -la | grep meta || echo "meta 已不存在"

      - name: Clone source repository
        id: clone_source
        run: |
          SOURCE_DIR="/tmp/meta-source-$(date +%s)"
          echo "source_dir=$SOURCE_DIR" >> $GITHUB_OUTPUT
          
          echo "正在克隆源仓库 meta 分支..."
          git clone --depth=1 --branch=meta https://github.com/MetaCubeX/meta-rules-dat.git "$SOURCE_DIR"
          
          echo "=== 源仓库根目录内容 ==="
          ls -la "$SOURCE_DIR/"
          echo "=== 源仓库子文件夹列表 ==="
          find "$SOURCE_DIR" -mindepth 1 -type d | sort

      - name: Force create and mirror sync new meta folder
        run: |
          SOURCE_DIR="${{ steps.clone_source.outputs.source_dir }}"
          
          echo "=== 开始全新镜像同步 ==="
          echo "创建新的 meta 文件夹..."
          mkdir meta
          
          echo "使用 rsync 强制镜像复制所有内容（带 --delete，确保精确一致）..."
          rsync -av --delete \
            --exclude='.git' \
            "$SOURCE_DIR/" "meta/"
          
          echo "✅ 新 meta 文件夹镜像同步完成"
          
          echo "=== 同步后 meta 根目录内容 ==="
          ls -la meta/
          echo "=== 同步后 meta 子文件夹列表 ==="
          find meta -mindepth 1 -type d | sort

      - name: Remove any residual .dat files
        run: |
          echo "额外清理可能的 .dat 文件..."
          DAT_FILES=$(find meta -name "*.dat" -type f 2>/dev/null | wc -l)
          if [ "$DAT_FILES" -gt 0 ]; then
            echo "发现 $DAT_FILES 个 .dat 文件，正在删除..."
            find meta -name "*.dat" -type f -delete
            echo "✅ 已清理 .dat 文件"
          else
            echo "✓ 无 .dat 文件（正常）"
          fi

      - name: Check for git changes
        id: check_git
        run: |
          echo "检查 Git 变化..."
          git add meta
          
          if git diff --cached --quiet; then
            echo "没有变化（已与源仓库完全一致）"
            echo "has_git_changes=false" >> $GITHUB_OUTPUT
          else
            CHANGED_COUNT=$(git diff --cached --name-only | wc -l)
            echo "检测到 $CHANGED_COUNT 个文件变化（包括多余文件删除和新文件同步）"
            echo "has_git_changes=true" >> $GITHUB_OUTPUT
            echo "changed_count=$CHANGED_COUNT" >> $GITHUB_OUTPUT

      - name: Commit and push changes
        if: steps.check_git.outputs.has_git_changes == 'true'
        run: |
          TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
          CHANGED_COUNT="${{ steps.check_git.outputs.changed_count }}"
          
          COMMIT_MSG="🔄 自动同步: 更新 MetaCubeX meta 文件夹
          
          强制删除旧 meta 并全新镜像同步，确保与源仓库完全一致（仅保留 asn、geo-lite、geo 三个文件夹）。
          
          详细信息:
          - 源仓库: MetaCubeX/meta-rules-dat@meta
          - 同步时间: $TIMESTAMP
          - 变化文件数: $CHANGED_COUNT（包含多余文件删除）
          
          由 GitHub Actions 自动镜像同步"
          
          git commit -m "$COMMIT_MSG"
          git push origin main
          
          echo "✅ 已提交并推送更新"

      - name: No changes needed
        if: steps.check_git.outputs.has_git_changes == 'false'
        run: |
          echo "🎉 已是最新版本，无需更新"
          echo "当前 meta 结构（仅三个文件夹）:"
          ls -la meta/
          echo "与源仓库完全一致 ✓"

      - name: Cleanup
        if: always()
        run: |
          echo "清理临时目录..."
          SOURCE_DIR="${{ steps.clone_source.outputs.source_dir }}"
          if [ -n "$SOURCE_DIR" ] && [ -d "$SOURCE_DIR" ]; then
            rm -rf "$SOURCE_DIR"
            echo "✅ 已清理临时目录"
          fi

      - name: Final verification
        run: |
          echo "=== 最终验证 ==="
          echo "同步完成时间: $(date)"
          
          if [ -d "meta" ]; then
            echo "meta 根目录内容（应仅三个文件夹）:"
            ls -la meta/
            
            if [ "$(ls -A meta | wc -l)" -eq 3 ]; then
              echo "✅ 验证通过: 仅 asn、geo-lite、geo 三个文件夹，与源仓库一致"
            else
              echo "⚠️  警告: meta 内文件/文件夹数量异常"
            fi
          else
            echo "❌ 错误: meta 目录未创建（严重问题）"
          fi
