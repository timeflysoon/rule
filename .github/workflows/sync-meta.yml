# 文件名: .github/workflows/delete-meta-folder.yml
# 删除 meta 文件夹工作流

name: Delete Meta Folder

on:
  workflow_dispatch:  # 仅手动触发

permissions:
  contents: write

jobs:
  delete-meta:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 1

      - name: Setup Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Delete meta folder
        run: |
          echo "🗑️  删除 meta 文件夹..."
          
          if [ -d "meta" ]; then
            echo "找到 meta 目录，正在删除..."
            rm -rf meta
            echo "✅ meta 目录已删除"
          else
            echo "ℹ️  meta 目录不存在，无需删除"
          fi

      - name: Check if there are changes
        id: check_changes
        run: |
          echo "📊 检查 Git 变化..."
          
          git add -A
          
          if git diff --cached --quiet; then
            echo "没有检测到变化"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            echo "检测到变化"
            echo "has_changes=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push if changes
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
          
          COMMIT_MSG="🗑️ 删除 meta 文件夹

          删除不再需要的 meta 文件夹。

          详细信息:
          - 删除时间: $TIMESTAMP
          - 操作: 删除整个 meta 目录

          由 GitHub Actions 手动触发执行"
          
          git commit -m "$COMMIT_MSG"
          git push origin main
          
          echo "✅ 已提交并推送删除操作"

      - name: No changes needed
        if: steps.check_changes.outputs.has_changes == 'false'
        run: |
          echo "ℹ️  没有变化需要提交（meta 文件夹可能已不存在）"

      - name: Final verification
        run: |
          echo "=== 操作完成 ==="
          echo "完成时间: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          
          if [ -d "meta" ]; then
            echo "❌ meta 文件夹仍然存在"
            exit 1
          else
            echo "✅ meta 文件夹已成功删除"
          fi
          
          echo ""
          echo "当前目录结构:"
          ls -la
