# 文件名: .github/workflows/sync-meta.yml
# 日常使用的同步工作流 - 与源仓库完全一致

name: Sync Meta Folders Only

on:
  schedule:
    - cron: '30 18 * * *'  # UTC 18:30 = 北京时间第二天 2:30
    - cron: '30 6 * * *'   # UTC 6:30 = 北京时间 14:30
  workflow_dispatch:  # 手动触发

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 2  # 应该很快完成
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 1

      - name: Setup Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Clone source repository
        id: clone_source
        run: |
          SOURCE_DIR="/tmp/meta-source-$(date +%s)"
          echo "source_dir=$SOURCE_DIR" >> $GITHUB_OUTPUT
          
          echo "📥 克隆源仓库..."
          git clone --depth=1 --branch=meta https://github.com/MetaCubeX/meta-rules-dat.git "$SOURCE_DIR"
          
          echo "源仓库内容 (最新状态):"
          ls -la "$SOURCE_DIR/"

      - name: Debug - Check source directory timestamps
        run: |
          SOURCE_DIR="${{ steps.clone_source.outputs.source_dir }}"
          
          echo "🔍 检查源仓库目录时间戳:"
          for dir in "$SOURCE_DIR"/*/; do
            if [ -d "$dir" ]; then
              dir_name=$(basename "$dir")
              # 使用stat获取目录修改时间
              dir_time=$(stat -c %y "$dir" 2>/dev/null || stat -f %Sm "$dir" 2>/dev/null || echo "unknown")
              file_count=$(find "$dir" -type f | wc -l)
              echo "  $dir_name: $file_count 个文件, 目录时间: $dir_time"
            fi
          done

      - name: Debug - Check current meta directory timestamps
        run: |
          echo "🔍 检查当前 meta 目录时间戳:"
          if [ -d "meta" ]; then
            for dir in meta/*/; do
              if [ -d "$dir" ]; then
                dir_name=$(basename "$dir")
                dir_time=$(stat -c %y "$dir" 2>/dev/null || stat -f %Sm "$dir" 2>/dev/null || echo "unknown")
                file_count=$(find "$dir" -type f | wc -l)
                echo "  $dir_name: $file_count 个文件, 目录时间: $dir_time"
              fi
            done
          else
            echo "  meta 目录不存在"
          fi

      - name: Force sync - Complete replacement
        run: |
          SOURCE_DIR="${{ steps.clone_source.outputs.source_dir }}"
          
          echo "🔄 强制同步 - 完全替换..."
          
          # 完全删除旧的 meta 目录
          rm -rf meta
          
          # 重新创建并复制所有内容
          mkdir -p meta
          
          echo "复制文件中..."
          
          # 复制所有文件和目录，但不包括 .git
          for item in "$SOURCE_DIR"/*; do
            # 排除 .git 和 .github 目录
            if [[ "$item" != *"/.git" && "$item" != *"/.github" ]]; then
              cp -r "$item" "meta/"
              echo "  已复制: $(basename "$item")"
            fi
          done
          
          echo "✅ 同步完成"

      - name: Verify sync result
        run: |
          echo "🔍 验证同步结果:"
          echo "同步后的 meta 目录:"
          ls -la meta/
          
          echo ""
          echo "各目录文件数量:"
          for dir in meta/*/; do
            if [ -d "$dir" ]; then
              dir_name=$(basename "$dir")
              file_count=$(find "$dir" -type f | wc -l)
              echo "  $dir_name: $file_count 个文件"
            fi
          done

      - name: Remove any .dat files
        run: |
          echo "🔍 检查 .dat 文件..."
          if find meta -name "*.dat" -type f 2>/dev/null | grep -q .; then
            echo "发现 .dat 文件，正在删除..."
            find meta -name "*.dat" -type f -delete
            echo "✅ 已删除 .dat 文件"
          else
            echo "✓ 没有 .dat 文件"
          fi

      - name: Check for git changes
        id: check_git
        run: |
          echo "📊 检查 Git 变化..."
          
          # 先查看状态
          echo "Git 状态:"
          git status --porcelain meta/ || true
          
          # 添加变化
          git add meta
          
          # 检查是否有待提交的更改
          if git diff --cached --quiet; then
            echo "ℹ️  没有检测到 Git 变化"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            CHANGED_COUNT=$(git diff --cached --name-only | wc -l)
            echo "📝 检测到 $CHANGED_COUNT 个文件变化"
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "changed_count=$CHANGED_COUNT" >> $GITHUB_OUTPUT
            
            echo "变化文件列表:"
            git diff --cached --name-only
          fi

      - name: Commit and push if changes found
        if: steps.check_git.outputs.has_changes == 'true'
        run: |
          TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
          CHANGED_COUNT="${{ steps.check_git.outputs.changed_count }}"
          
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            SYNC_TYPE="手动同步"
          else
            SYNC_TYPE="定时同步"
          fi
          
          COMMIT_MSG="🔄 $SYNC_TYPE: 更新 Meta 规则文件夹

          与源仓库 MetaCubeX/meta-rules-dat@meta 分支保持同步。

          详细信息:
          - 同步时间: $TIMESTAMP
          - 同步类型: $SYNC_TYPE
          - 变化文件数: $CHANGED_COUNT
          - 触发方式: ${{ github.event_name }}

          由 GitHub Actions 自动执行"
          
          echo "提交信息:"
          echo "$COMMIT_MSG"
          
          git commit -m "$COMMIT_MSG"
          git push origin main
          
          echo "✅ 已提交并推送更新"

      - name: Create empty commit if no changes (for debugging)
        if: steps.check_git.outputs.has_changes == 'false'
        run: |
          echo "⚠️  没有检测到文件变化，但同步已执行"
          echo "创建空提交用于调试..."
          
          TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
          
          COMMIT_MSG="🔍 调试: 同步检查

          同步检查完成，但未检测到文件变化。

          详细信息:
          - 检查时间: $TIMESTAMP
          - 触发方式: ${{ github.event_name }}
          - 状态: 文件已是最新或同步未产生变化

          由 GitHub Actions 自动执行"
          
          git commit --allow-empty -m "$COMMIT_MSG"
          git push origin main
          
          echo "✅ 已创建调试提交"

      - name: Cleanup
        if: always()
        run: |
          echo "🧹 清理临时文件..."
          SOURCE_DIR="${{ steps.clone_source.outputs.source_dir }}"
          if [ -n "$SOURCE_DIR" ] && [ -d "$SOURCE_DIR" ]; then
            rm -rf "$SOURCE_DIR"
            echo "✅ 已清理临时目录"
          fi

      - name: Final summary
        run: |
          echo "=== 同步任务完成 ==="
          echo "完成时间: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          
          if [ "${{ steps.check_git.outputs.has_changes }}" = "true" ]; then
            echo "✅ 结果: 已同步并提交 ${{ steps.check_git.outputs.changed_count }} 个文件更新"
          else
            echo "⚠️  结果: 未检测到文件变化"
          fi
          
          echo ""
          echo "meta 目录最终状态:"
          if [ -d "meta" ]; then
            ls -la meta/
          fi
          
          echo ""
          echo "任务状态: 完成 ✓"
